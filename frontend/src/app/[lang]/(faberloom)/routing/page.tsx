import ViewPlaceholder from "@/components/shell/ViewPlaceholder";

export default function RoutingPage() {
  return (
    <ViewPlaceholder
      title="IA: modelos y ruteo"
      subtitle="Routing Factory · keys, cost caps y reglas del tenant"
      icon="🎛"
    />
  );
}
