import ViewPlaceholder from "@/components/shell/ViewPlaceholder";

export default function InboxPage() {
  return (
    <ViewPlaceholder
      title="Inbox"
      subtitle="Entrada global · clasificada · asigná, marcá o creá draft"
      icon="✉"
    />
  );
}
